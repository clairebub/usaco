# 1. read in the words from the input file
# 2. count the letter of alphabet in the words
# 3. print the count, starting from 'a', and end to 'z'
#    hint: ord('a') => 97, chr(97) => 'a'
#    basically ord() function takes a character and gives its order in
#    ascii, where chr() function is the inverse, take an integer represents
#    its order in ascii table and gives its character 
